/**
 * UI and Animation Constants for Pokemon Features
 */

// Animation timings (in milliseconds)
export const ANIMATION_DELAY_MS = 100;
export const CAROUSEL_TRANSITION_MS = 500;
export const SEARCH_DEBOUNCE_MS = 300;

// Stats
export const DEFAULT_MAX_STAT = 100;
export const SEARCH_RESULTS_LIMIT = 10;

// Cache configuration (in milliseconds)
export const SEARCH_CACHE_TIME_MS = 5 * 60 * 1000; // 5 minutes
